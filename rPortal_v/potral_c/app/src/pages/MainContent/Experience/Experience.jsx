// モックコンポーネント読み込み
import { MockMessage, MockLayoutBox } from "@/mock/Components";

export const Experience = (props) => {
    return (
        <MockLayoutBox>
            <MockMessage message="Experience" />
        </MockLayoutBox>
    );
}
